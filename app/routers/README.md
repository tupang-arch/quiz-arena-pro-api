# Quiz Arena Pro

FastAPI + PostgreSQL backend project (portfolio-ready).

## Run with Docker
1) Create `.env` from `.env.example`
2) Start:
   docker compose up --build

Open:
- http://localhost:8000/
- http://localhost:8000/docs